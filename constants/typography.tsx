import Colors from "./colors";

const Typography = {
  title: {
    fontSize: 24,
    fontWeight: 700,
    color: Colors.textColor,
  },
  subtitle: {
    fontSize: 18,
    fontWeight: 600,
    color: Colors.textColor,
  },
  body: {
    fontSize: 14,
    fontWeight: 400,
    color: Colors.textColor,
  },
};

export default Typography;
